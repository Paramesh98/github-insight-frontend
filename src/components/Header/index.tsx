const Header = () => (
  <header className='py-3 bg-dark text-light text-center'>
    <h1>GitHub User Info</h1>
    <p>Retrieve repositories, gists, and organizations for any GitHub user</p>
  </header>
);

export default Header;
